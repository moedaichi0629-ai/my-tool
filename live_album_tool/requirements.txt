# ライブ思い出アルバムツール - 必要なライブラリ
# インストール方法: pip install -r requirements.txt

# Google APIとの認証・通信に必要なライブラリ群
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.0.0

# LINE Messaging API（LINEボット開発用）
line-bot-sdk>=3.11.0

# Webサーバー（LINEのWebhookを受け取るために使用）
flask>=3.0.0

# .envファイルから環境変数を読み込むために使用
python-dotenv>=1.0.0
